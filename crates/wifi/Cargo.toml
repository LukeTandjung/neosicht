[package]
name = "wifi"
version = "0.1.0"
edition = "2024"
publish = false
build = "src/adapters/core_wlan_build.rs"

[dependencies]
base-gpui = { workspace = true }
gpui = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
theme = { workspace = true }
