[package]
name = "music"
version = "0.1.0"
edition = "2024"
publish = false
build = "src/adapters/applescript_build.rs"

[dependencies]
base64 = "0.22"
base-gpui = { workspace = true }
gpui = { workspace = true }
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
theme = { workspace = true }
